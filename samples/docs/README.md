# Sample documents

Drop source documents here for future ingest testing.

Sample files themselves are not committed to git — only this README. Add patterns to `.gitignore` as needed when ingest is implemented.
